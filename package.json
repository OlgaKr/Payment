{
  "name": "payment-testing-course",
  "version": "1.0.0",
  "description": "Курс: Тестування платіжних систем",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:unit": "jest --testPathPattern=unit",
    "test:integration": "jest --testPathPattern=integration",
    "test:contract": "jest --testPathPattern=contract"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "stripe": "^14.0.0",
    "ajv": "^8.12.0"
  }
}
